import { AddImagesToProductsPipe } from './add-images-to-products.pipe';

describe('AddImagesToProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new AddImagesToProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
